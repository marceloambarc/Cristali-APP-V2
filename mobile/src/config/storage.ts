const STORAGE_NAME = '@CRISTALIAuth';

const COLLECTION_ITEMS = `${STORAGE_NAME}:items`;
const COLLECTION_USER = `${STORAGE_NAME}:user`;
const COLLECTION_TOKEN = `${STORAGE_NAME}:token`;
const COLLECTION_DEVICE_TOKEN = `${STORAGE_NAME}:devicetoken`;
const COLLECTION_PASSWORD = `${STORAGE_NAME}:password`;

export {
  COLLECTION_USER,
  COLLECTION_TOKEN,
  COLLECTION_ITEMS,
  COLLECTION_DEVICE_TOKEN,
  COLLECTION_PASSWORD
}